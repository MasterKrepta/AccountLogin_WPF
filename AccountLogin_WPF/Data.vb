Imports System.Data.SQLite
Public Class Data

    'Dim conn As New SQLiteConnection("Data Source =")
    'conn.Open()

    'Dim cmd As New SQLiteCommand
    'cmd.connection = conn

    'cmd.CommandText = "Select * from ####"

    'Dim rdr As SQLiteDataReader = cmd.ExecuteReader
    'Dim dt As New DataTable
    'dt.load(rdr)
    'rdr.close()
    'conn.Close()
    'grid1.dataSource = dt

End Class
